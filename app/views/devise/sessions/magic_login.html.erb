<h2>ログイン</h2>

<%= form_with url: send_magic_link_path, method: :post, local: true do |f| %>
  <div>
    <%= f.label :email, "メールアドレス" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div>
    <%= f.submit "ログインリンクを送信" %>
  </div>
<% end %>