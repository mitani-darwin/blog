<%= form_with url: magic_login(:user), method: :post, local: true do |f| %>
  <div>
    <%= f.label :email, "メールアドレス" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div>
    <%= f.submit "ログインリンクを送信" %>
  </div>
<% end %>